import React, { Component } from 'react';
import { Container } from '@material-ui/core';

class Profile extends Component {
    state = {  }
    render() { 
        return ( <React.Fragment>
            <Container>
                Hello You are on Profile.
            </Container>
        </React.Fragment> );
    }
}
 
export default Profile;